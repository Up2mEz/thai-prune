# Attempt 3 pre-submission engineering evidence

- `dataset_creation.json` records the private Dataset identity, version, and
  byte/hash identities supplied to Kaggle.
- `validation_failure.json` records the remote archive-expansion mismatch and
  confirms zero `SaveKernel` and scientific calls.
- Generated local staging and read-only remote metadata are preserved under
  `runs/kaggle/datasets/` and remain excluded from Git.
- No scientific predictions or metrics are present.

