# Attempt 1 engineering evidence

- `FAILURE.json` is copied byte-for-byte from the private Kaggle version-1
  output.
- SHA-256:
  `e05142cfe5cfb14b8946a28028dceb34efae4370e5939adc166e060f4b8c8423`.
- The downloaded output also contained a kernel log. It remains in the ignored
  local run directory and was not opened because the failure record was
  sufficient to classify the pre-inference failure.
- No raw scientific-output artifact was present in the downloaded output.
